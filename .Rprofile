# Set options for renv convenience
options(
  repos = c(
    CRAN = "https://cloud.r-project.org",
    IHTM = "https://oxfordihtm.r-universe.dev"
  ),
  renv.config.auto.snapshot = FALSE,      ## Don't update lock automatically
  renv.config.rspm.enabled = TRUE,        ## Use RSPM for linux
  renv.config.install.shortcuts = FALSE,  ## Use existing local library
  renv.config.cache.enabled = TRUE        ## Use renv build cache
)

# Activate renv
source("renv/activate.R")
